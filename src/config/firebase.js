// Firebase initialization for React Native
import { firebase } from '@react-native-firebase/app';

// Optionally, you can add more Firebase services here (auth, firestore, etc.)
// import '@react-native-firebase/auth';
// import '@react-native-firebase/firestore';

// No manual config needed if google-services.json is in android/app
// For iOS, use GoogleService-Info.plist in ios/ directory

export default firebase;
